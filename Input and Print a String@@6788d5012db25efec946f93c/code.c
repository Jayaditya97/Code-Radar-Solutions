#include <stdio.h>

int main() {
    char str[100];  // Declare a string array to store the input
    
    // Take input from the user
    scanf("%s", str);
    
    // Print the entered string
    printf("You entered: %s\n", str);
    
    return 0;
}
