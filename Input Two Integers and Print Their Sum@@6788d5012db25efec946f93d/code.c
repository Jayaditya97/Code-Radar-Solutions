#include <stdio.h>

int main() {
    int num1, num2;
    
    // Take input of two integers from the user
    scanf("%d %d", &num1, &num2);
    
    // Print the sum of the two integers
    printf("Sum: %d\n", num1 + num2);
    
    return 0;
}