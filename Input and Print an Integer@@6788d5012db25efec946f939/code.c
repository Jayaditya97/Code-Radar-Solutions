#include <stdio.h>

int main() {
    int num;
    
    // Read an integer from the user
    scanf("%d", &num);
    
    // Print the entered integer
    printf("You entered: %d\n", num);
    
    return 0;
}