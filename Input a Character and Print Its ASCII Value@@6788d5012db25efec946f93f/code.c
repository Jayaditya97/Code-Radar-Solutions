#include <stdio.h>

int main() {
    char ch;
    
    // Take input of a single character
    scanf("%c", &ch);
    
    // Print the ASCII value of the character
    printf("ASCII Value: %d\n", ch);
    
    return 0;
}