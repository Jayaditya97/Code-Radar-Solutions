#include <stdio.h>
int main() {
    char ch;
    // Read a single character from the user
    scanf("%c", &ch);
    // Print the entered character
    printf("You entered: %c\n", ch);
    return 0;
}